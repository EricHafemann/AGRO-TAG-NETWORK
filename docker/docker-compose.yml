version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: agro-tag-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: agro_tag_network
    ports:
      - "3306:3306"
    volumes:
      - agro_tag_data:/var/lib/mysql

volumes:
  agro_tag_data: